class Driver {
  constructor(name, startDate) {
    this.name = name
    this.startDate = startDate
  }

  startDate() {
    let d = Date() instanceof startDate
  }

  yearsExperienceFromBeginningOf(year) {
    return year - startDate
  }
}
